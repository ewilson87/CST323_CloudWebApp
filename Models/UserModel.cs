using Microsoft.AspNetCore.Identity;

namespace CloudWebApp.Models
{
    //uses IdentityUser for username, password, email, etc.
    public class UserModel : IdentityUser
    {
        
        public UserModel()
        {
        }
    }
}
